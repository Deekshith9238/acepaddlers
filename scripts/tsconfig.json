{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"]
  },
  "include": ["src"],
  "comment": "seed.ts imports frontend data files cross-package and is run via tsx, not typechecked here",
  "exclude": ["src/seed.ts"]
}
