# CloudFront was abandoned because the AWS account is not verified for CloudFront
# distribution creation. The custom domain is served via TLS on the ALB instead
# (the API container serves the SPA). See https.tf.
